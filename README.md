# FileScan

A Python tool that identifies file types using magic numbers.

## Features
- Magic number detection
- CLI scanning
- Detect fake extensions

You can add more file signature in ###"data/file_sigs.json"

## Usage
python cli.py suspicious_file.bin